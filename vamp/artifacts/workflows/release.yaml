kind: workflows
name: release
breed: release
schedule: daemon
environment_variables:
  WINDOW: 60s
dialects:
  kubernetes:
    # serviceAccountName: vamp-quantification
    imagePullSecrets:
      - name: regsecret
